---
# vars file for ansible-role-dotfiles

user_home: "{{ lookup('env','HOME') }}"
user_group: users
vim_config: "{{ user_home }}/.vimrc"
dotfiles:
  - '.shellpaths'
  - '.shellvars'
  - '.zshenv'
zsh_config: "{{ user_home }}/.zshrc"
zsh_custom: "{{ zsh_ohmyzsh }}/custom"
zsh_ohmyzsh: "{{ user_home }}/.oh-my-zsh"
zsh_theme: "spaceship"
